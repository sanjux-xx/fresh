Flask==3.0.3
requests==2.32.3
google-search-results==2.4.2
sentry-sdk[flask]==2.14.0
gunicorn==22.0.0

# app.py imports markupsafe.escape directly (block pages), so it is a direct
# dependency, not just a Flask transitive one. Pinned to the version tested.
markupsafe==3.0.3
